name: Test leptosfmt action

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  leptosfmt-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: test default
        uses: ./
